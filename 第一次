echo "# Calculator Project" > README.md
echo '# 简单计算器' >> README.md

echo 'def add(a, b):
    return a + b

if __name__ == "__main__":
    print("Calculator is ready")' > calculator.py

# 查看状态
git status

# 添加并提交
git add README.md calculator.py
git commit -m "Initial commit: add project structure and base calculator"

# 查看提交记录
git log --oneline
